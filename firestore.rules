rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to scores collection
    match /scores/{document} {
      allow read, write: if true;
    }
    
    // Allow read and write access to test collection
    match /test/{document} {
      allow read, write: if true;
    }
  }
}
